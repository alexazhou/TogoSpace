[pytest]
asyncio_mode = auto
python_files = test.py test_*.py
xfail_strict = true
addopts =
    --cov=src
    --cov=tui
    --cov-config=.coveragerc
    --cov-report=term-missing:skip-covered
    --cov-report=xml
markers =
    forked: run this test item in a dedicated subprocess
