name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-latest
    timeout-minutes: 5
    strategy:
      matrix:
        python-version: ["3.11"]

    steps:
    - uses: actions/checkout@v6
      with:
        lfs: true

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip dependencies
      uses: actions/cache@v5
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt pytest-timeout

    - name: Run mypy
      run: |
        bash scripts/run_mypy.sh

    - name: Run unit tests
      run: |
        PYTHONPATH=src python -m pytest tests/unit/ -v --timeout=30

    - name: Run API tests
      run: |
        PYTHONPATH=src python -m pytest tests/api/ -v --timeout=30

    - name: Run integration tests
      run: |
        PYTHONPATH=src python -m pytest tests/integration/ -v --timeout=30

    - name: Run real tests
      run: |
        PYTHONPATH=src python -m pytest tests/real/ -v --timeout=30
